# Живое состояние сессии (не история — только текущий срез)

Обновляется по факту, перечитывается сразу после каждой компакции диалога.
Правило и объяснение — в `AI-WORKFLOW.md`, раздел «Живой приоритет задач
переживает сжатие диалога».

Последнее обновление: 19.09.2026, ночная марафонская сессия.

## Открытые линии, по приоритету

1. **Значок «перемешать» (i-shuffle) — исправлен, не закоммичен.** Замерено
   getBBox() дважды (локально и на живом сайте): контур занимал 40.4% рамки
   24×24 против 62.5% у i-repeat — реальная, не иллюзорная разница. Заменён на
   проверенный контур Feather/Lucide «shuffle» → теперь 53.1%. Ждёт коммита
   вместе с остальным пакетом иконок ниже (index.html).
2. **Пакет «иконки Конструктора» — готов, не закоммичен.** `.forgeChip
   [data-sort="top"] .ic{fill:currentColor}` (index.html), плюс js/forge.js:
   new/top/trending чипы — currentColor+style вместо голого stroke/fill атрибута
   (та же беда `.ic{fill:none}` перебивает), «new» — плюсик заменён на
   колокольчик (Lucide bell), свой цвет #6cc3ff. Верено локально и на живом
   сайте (getComputedStyle). Полный guard.mjs ещё не прогнан начисто (предыдущий
   прогон дал ложные page.goto-таймауты — конкуренция ресурсов с открытой
   браузерной панелью, не реальные баги; препросмотр остановлен, нужен чистый
   повтор перед коммитом).
3. **Баг: рисунок пути «Эстафеты» не появляется после пройденного этапа**
   (жалоба владельца, скрин hero-карточки без рисунка + скрин статуса с
   чужими цепочками QA-TEST/MR.V в статусе «ждёт этап 2/3»). НЕ исследовано —
   нужно найти, где рисуется путь для карточки relay и почему для эстафеты он
   не рисуется как для Caravan/Score Attack.
4. **Баг: видео не сохраняются при рекорде.** Владелец видит только «ваш первый
   полёт» в галерее видео, у Caravan есть реальный рекорд (5518 очков, виден на
   карточке), но карточка видео для Caravan говорit «пока нет рекорда». Похоже
   связано с открытой незавершённой миграцией «Кино полёта» (см. память
   `project_cinema_koltsevoy_dvizhok_otlozheno_18_09` — first-flight/highlight
   всё ещё на старом mp4-muxer, экспорт мигрирует на mediabunny). НЕ
   исследовано.
5. **Фича «одноразовые подсказки на иконках» — решение принято, макет не
   построен.** Гибрид (таймер + можно тапнуть раньше), 4-5 секунд. Нужен макет
   (новый визуальный элемент → закон макета) с реальным текстом подсказок,
   прежде чем код. См. `RESEARCH-2026-09-ODNORAZOVYE-PODSKAZKI-EDINOOBRAZIE.md`
   — там же разбор, что 5 из 6 существующих подсказок сейчас БЕЗ таймера
   вообще (гасятся действием), стандартизация — отдельное решение, тоже не
   реализовано.
6. **Вопрос про звезду/сердце на карточке Мастерской** (владелец: «только на
   одной карте могу поставить свою звезду») — задан, не исследован, не забыт.

## Что уже закрыто в этой сессии (не трогать повторно)

- COSMOGRAM-бейдж в окне «крупно» (Коллекция) — измерено, поправлено, гвард 232+316 зелёные.
- Экран «Спасибо»: текст без точки, плейсхолдер по центру — закоммичено (6057884).
- Единая карточка-контейнер (`.grUnified` = `.setPanel`) + вуаль неба у Хартии/Спасибо — закоммичено (17d7930).
- Кнопки SSG/RSG (размер пилюли) — измерено дважды, боксы идентичны, разница
  признана оптической иллюзией золотой рамки, владелец сказал оставить как есть.
