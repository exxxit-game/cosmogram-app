# Живое состояние сессии (не история — только текущий срез)

Обновляется по факту, перечитывается сразу после каждой компакции диалога.
Правило и объяснение — в `AI-WORKFLOW.md`, раздел «Живой приоритет задач
переживает сжатие диалога».

Последнее обновление: 19.09.2026, ночная марафонская сессия (продолжается).

## Открытые линии, по приоритету

1. **Новое, не исследовано: «кнопка Назад» в тестовом браузере (localhost:8934,
   без обёртки Telegram) — владелец видит её и связывает с давней жалобой
   («моя кнопка вылезала из-под кнопок телеграма»).** Нужно уточнить, на
   каком именно экране/состоянии он её видит (сейчас открыт зум-модал
   Flocculent — там кнопка «✕ Закрыть», не «← Назад») — не додумывать,
   спросить/попросить скрин, прежде чем чинить.
2. **Открыто, не диагностировано: лишний отступ над плашкой имени явления**
   (окно «крупно», Коллекция). Цепочка отступов: `#angarPvZoomCard{padding-top:
   var(--menu-top-pad)}` + `#angarPvZoomName{margin:16px 46px 0}`
   (index.html:1858-1871). Живой замер на телефоне (19.09.2026): полный зазор
   118.5 CSS px, из них `--sat-menu` формула даёт 78.5px (Telegram НЕ в
   fullscreen — резерв под родную шапку ⌄/⋮, вероятно корректен по замыслу
   13.09.2026, не обязательно баг). НЕ предлагать число без ответа владельца.
3. **Новая идея (владелец, не решено): переразместить заголовок явления.**
   Сейчас — большая отдельная карточка под шапкой Telegram. Владелец хочет
   разобрать варианты компактнее — вписать часть заголовка между кнопками
   Telegram «Назад»/«Свернуть», как уже сделано с HUD. Явно предупредил: есть
   очень длинные названия явлений, учесть это, не тестировать только на
   коротких. Нужен макет (новый визуальный элемент), не прямая правка.
4. **Новая идея (владелец, текст выбран, макет не построен): угловая лента
   вместо мелкой звезды-иконки на карточках Мастерской.** Звезда-переключатель
   («Выбор автора») остаётся только для владельца; обычный игрок вместо неё
   видит угловую ленту (референс — диагональная лента типа «NEW», левый
   верхний угол карточки сейчас пустой). Текст ленты — «ЗВЕЗДА АВТОРА» (RU),
   владелец подтвердил, из вариантов (МАЯК/ПУТЕВОДНАЯ/ОРИЕНТИР/ЭТАЛОН/
   ЗВЕЗДА АВТОРА — МАЯК отклонён явно, «ничего не говорит человеку»). Учесть
   при макете: самый длинный перевод — французский «ÉTOILE DE L'AUTEUR»
   (18 символов против 13 у RU/EN) — проверять ленту по нему, не только по
   русскому. Нужен макет, не прямая правка.
5. **Новая идея (владелец, не решено): мотивационная карточка «создать своё
   первое небо».** В ряду значков Конструктора (колокольчик/искра/рост/
   сердце/кубик, см. скрин 19.09.2026 21:02) добавить последним значком
   мотивацию создать свой первый трек — пока у игрока нет ни одного своего,
   текст в духе «У тебя ещё нет своих небес — создай первое» + кнопка на
   «Создать». Сейчас неудобно найти созданные треки. Нужен макет.
6. **Backlog, не сейчас:** лунная тема для «Неба месяца» (луна меняется
   каждый месяц, можно привязать); пересмотр порядка режимов на главном
   экране (владелец недоволен текущей расстановкой); описания карточек
   режимов — качественный редрайт (Стюарт Чейз, «Тирания слов»).

## Что закрыто в этой сессии (не трогать повторно)

- COSMOGRAM-бейдж в окне «крупно» (Коллекция) — гвард 232+316 зелёные, закоммичено (94b6581).
- Экран «Спасибо»: текст без точки, плейсхолдер по центру — закоммичено (6057884).
- Единая карточка-контейнер (`.grUnified`=`.setPanel`) + вуаль неба у Хартии/Спасибо — закоммичено (17d7930).
- Значки Конструктора (top-заливка/new-колокольчик/trending) + i-shuffle контур (Спидран SSG/RSG) — закоммичено (e685756).
- Живое состояние сессии + крючки SessionStart(compact)/guard-в-фоне/Windows-путь/broad-taskkill
  — закоммичено (7f9af70, a8537e7, 4c4ff83, 6c02b28).
- **Диагноз «рисунок пути Эстафеты»** — НЕ баг, осознанное решение (relay намеренно не участвует
  в «покажи свой рекорд», очки этапа — унаследованные, не личный рекорд). js/ui.js:534-535,971-981.
- **Баг «видео не сохраняются при рекорде» — исправлен и закоммичен** (52ef566 app, 984663a crew).
  Корень: grab() (js/cinema.js) сам планировал себя отдельным requestAnimationFrame — теперь
  вызывается из render.js:loop() сразу после реального draw(). Подтверждено живым замером на
  подключённом телефоне: было 0 кадров/300мс, стало ~19.4 кадра/сек. Стражи 267/300 переписаны
  и зелёные. Пустая карточка видео Эстафеты в галерее — отдельное, осознанное решение (не баг).
- **Кнопки SSG/RSG (размер пилюли)** — измерено, боксы идентичны, разница признана оптической
  иллюзией золотой рамки, владелец подтвердил «оставить как есть».
- **Значок i-shuffle мельче i-repeat** — РЕАЛЬНЫЙ баг (не иллюзия), подтверждено getBBox()
  дважды: 40.4% рамки против 62.5%. Заменён на контур Feather/Lucide, закоммичено (e685756).
- **Flocculent (id=65) рукава жались к плашке COSMOGRAM — исправлено ДВУМЯ последовательными
  фиксами, закоммичено (771f642, 8a8b803):**
  1. Убран лишний `ANGAR_PV_ZOOM_EXTRA_DROP[65]=28` (собственная выдумка ранней сессии).
  2. Настоящая причина места «справа от плашки» — холст физически кончался на границе плашки,
     там просто негде было рисовать. Найдено ТОЧНО через номерованную схему-оверлей с реальными
     координатами (квадрат [2]) — см. новое ABSOLUTE-правило
     [[feedback_numerovannye_ramki_vmesto_slov]], владелец назвал его базовым. Холст теперь
     физически выше (измеренная высота ряда плашки прибавляется в angarPvZoomDraw), плашка
     красится поверх холста в DOM-порядке, ничего дополнительно не вырезается. Живая ловушка,
     пойманная ДО показа: мерить высоту от самого холста нельзя (сам себя растит каждый кадр,
     7956px за пару секунд) — мерить от `#angarPvZoomStage`. Стражи 316/232 зелёные.
  Урок записан — feedback_19_09_marafon_razbor_oshibok.md Failure 5 (сиблинг-число пережило
  первый фикс) + новое базовое правило про номерованные схемы.

## Технические hook'и, добавленные этой ночью (владелец: «везде, где я уже заебался объяснять»)

`.claude/settings.json` + `.claude/hooks/`: guard-full-suite-warn.sh (фоновый полный прогон
стражей — спрашивает), no-windows-path-redirect.sh (`C:\...` в redirect — спрашивает),
no-broad-taskkill.sh (`taskkill /IM` — блокирует, `/PID` — пропускает), reinject-session-state.sh
(SessionStart/compact — техническое, не текстовое, восстановление живого состояния). Все
проверены на реальных сценариях до коммита, не все правила памяти хукуемы механически (часть
требует понимания смысла разговора).

## Инцидент: завис ноут (19.09.2026, ~40 минут)

Фоновый полный прогон стражей (PID 1748) висел в фоне без присмотра — то же самое, что уже было
17.09 и было записано как ABSOLUTE-правило, но не применено вовремя. Убит через `tasklist`+
`taskkill /F`, машина отпустило сразу. Полного прогона стражей в этой сессии больше нет — только
`--only=` под конкретную правку. Теперь технически защищено hook'ом (см. выше).

## Технический факт про подключённый телефон (cf3beda0, OPPO CPH2631)

`adb screencap` даёт чёрный кадр на экране блокировки (FLAG_SECURE — ограничение Android, не
баг инструмента). CDP eval работает независимо от блокировки экрана. `screen_off_timeout`
увеличен до 1 часа. При проверках через CDP eval — ВСЕГДА сначала `toMenu(); renderHangar();
setScreen('hangar');` перед `angarPvZoomOpen(...)`, иначе `angarPvZoomDraw()` тихо не рисует
(проверка `screenName!=='hangar'` в самом начале функции) — поймано дважды подряд этой ночью.
