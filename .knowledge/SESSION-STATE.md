# Живое состояние сессии (не история — только текущий срез)

Обновляется по факту, перечитывается сразу после каждой компакции диалога.
Правило и объяснение — в `AI-WORKFLOW.md`, раздел «Живой приоритет задач
переживает сжатие диалога». Механизм восстановления технический (SessionStart
hook, `.claude/hooks/reinject-session-state.sh`), не полагается на память ИИ.

Последнее обновление: 19.09.2026, ночная марафонская сессия (продолжается,
владелец ожидает скорое сжатие диалога — этот файл написан специально под это).

## Открытые линии, по приоритету

1. **Макет ленты «Звезда автора» одобрен по виду, но БЕЗ явного «да» на
   внесение в игру.** Владелец: «мне очень нравится, хорошая работа» — это
   похвала макету, не разрешение кодить (закон макета: только явное «да» на
   ИМЕННО внесение, см. [[feedback_macet_only_for_new_visual_design]]).
   Макет: `.knowledge/macets/masterskaya-lenta-zvezda-avtora-19-09-2026.html`
   (отправлен владельцу). Текст «ЗВЕЗДА АВТОРА», 2 строки, золотой язык
   плашки (#dba93c/#f0c040), проверено на трёх языках (RU/EN коротко, FR
   самый длинный — «ÉTOILE DE L'AUTEUR», умещается). Ждать явного «да» на
   внесение, отдельно от «нравится макет».
2. **Новое, не исследовано: «кнопка Назад» в тестовом браузере (localhost:8934,
   без обёртки Telegram) — владелец видит её и связывает с давней жалобой
   («моя кнопка вылезала из-под кнопок телеграма»).** Нужно уточнить, на
   каком именно экране/состоянии он её видит (в модалке зума Flocculent была
   кнопка «✕ Закрыть», не «← Назад» — не то же самое) — не додумывать,
   спросить/попросить скрин, прежде чем чинить. Владелец сам сказал «пока не
   будем с этим» — отложено по его слову, не забыто.
3. **Новое, не диагностировано: собственная кнопка «✕ Закрыть» (`#angarPvZoomClose`,
   окно «крупно») визуально частично спрятана за родными ⌄/⋮ Telegram** — замечено
   на живых скринах 19.09.2026 (маленький синий блик под точками), не тот же баг,
   что заголовок (см. закрыто ниже). Не диагностировано глубже, не додумано — нужен
   отдельный живой замер (adb+CDP), прежде чем чинить.
4. **Новая идея (владелец, не решено): мотивационная карточка «создать своё
   первое небо».** В ряду значков Конструктора (колокольчик/искра/рост/
   сердце/кубик) добавить последним значком мотивацию создать свой первый
   трек — пока у игрока нет ни одного своего, текст в духе «У тебя ещё нет
   своих небес — создай первое» + кнопка на «Создать». Сейчас неудобно найти
   созданные треки. Нужен макет.
5. **Backlog, не сейчас:** лунная тема для «Неба месяца» (луна меняется
   каждый месяц, можно привязать); пересмотр порядка режимов на главном
   экране (владелец недоволен текущей расстановкой); описания карточек
   режимов — качественный редрайт (Стюарт Чейз, «Тирания слов»).

## Что закрыто в этой сессии (не трогать повторно, всё закоммичено, НЕ запушено)

- COSMOGRAM-бейдж в окне «крупно» (Коллекция) — гвард 232+316 зелёные (94b6581).
- Экран «Спасибо»: текст без точки, плейсхолдер по центру (6057884).
- Единая карточка-контейнер (`.grUnified`=`.setPanel`) + вуаль неба у Хартии/Спасибо (17d7930).
- Значки Конструктора (top-заливка/new-контур/trending) + i-shuffle контур SSG/RSG (e685756).
- Живое состояние сессии + 4 технических hook'а (7f9af70, a8537e7, 4c4ff83, 6c02b28) — см. ниже.
- **Диагноз «рисунок пути Эстафеты»** — НЕ баг, осознанное решение (relay намеренно не участвует
  в «покажи свой рекорд», очки этапа унаследованные, не личный рекорд). js/ui.js:534-535,971-981.
- **Баг «видео не сохраняются при рекорде» — исправлен** (52ef566 app, 984663a crew). Корень:
  grab() (js/cinema.js) сам планировал себя requestAnimationFrame — теперь вызывается из
  render.js:loop() сразу после реального draw(). Подтверждено на телефоне: было 0 кадров/300мс,
  стало ~19.4/сек. Стражи 267/300 переписаны, зелёные. Пустая карточка видео Эстафеты — отдельное
  осознанное решение, не баг.
- **Кнопки SSG/RSG (размер пилюли)** — боксы идентичны, разница — оптическая иллюзия золотой
  рамки, владелец подтвердил «оставить как есть».
- **i-shuffle мельче i-repeat** — РЕАЛЬНЫЙ баг, подтверждено getBBox() (40.4% рамки против
  62.5%). Заменён на контур Feather/Lucide (e685756).
- **Flocculent (id=65) рукава жались к плашке COSMOGRAM** — ДВА фикса (771f642, 8a8b803):
  убран собственный `ANGAR_PV_ZOOM_EXTRA_DROP[65]=28`; холст физически стал выше (измеренная
  высота ряда плашки), плашка красится поверх в DOM-порядке. Найдено через номерованную
  схему-оверлей — новое ABSOLUTE-правило [[feedback_numerovannye_ramki_vmesto_slov]], владелец
  назвал его базовым. Живая ловушка (мерить от `#angarPvZoomStage`, не от самого холста —
  иначе бесконечный рост). Стражи 316/232 зелёные.
- **Звезда «Выбор автора» кликабельна только на одной карточке из восьми — исправлено** (62cf844).
  Корень найден пошагово (сессия/подпись токена — ОБЕ верны, проверено HMAC-запросом к БД) до
  настоящей причины: `workshopList()` (js/sync.js:754) слал личность (`syncAuth()`) ТОЛЬКО при
  sort==='mine', на остальных сортах — вообще без личности, хотя сервер ожидает её на любом
  сорте. Подтверждено патчем функции прямо в памяти боевой страницы (реальный Telegram-вход):
  isOwner стал true, скрытых звёзд 0 из 8 вместо 7 из 8. Стражи Workshop (19 шт) зелёные.
- **Колокольчик «new» — уменьшен и залит** (e35e6a3). Был крупнее соседей (18.1×19.5 из 24×24
  против ♥16×15.2/✨16.4×16.4) и пустой (stroke, не fill). Взят готовый залитый контур
  (Heroicons bell solid), масштаб 0.84 от центра → 15.2×16.4, совпадает с соседями. Гвард 176
  (guardWorkshopTrendingStarAndChipWidths) зелёный.
- **Заголовок явления (окно «крупно») — компактный отступ «Вариант Б», наложение на «✕ Закрыть»
  устранено** (a9bc573 app, 9527d9f crew). Настоящая причина ГОРИЗОНТАЛЬНАЯ, не вертикальная:
  margin `#angarPvZoomName` был 46px, кнопка `#angarPvZoomClose` реально резервирует 48px —
  недохват 2px, ловится на самой широкой строке длинных названий (5 из 7 явлений cosmos).
  Живой замер adb+CDP подтвердил и баг (name.right=314 vs close.left=312), и фикс (46→54px,
  зазор −2px→+6px). После фикса вертикальный отступ безопасно вернулся на компактный
  `--sat-menu+6px` («Вариант Б», владелец явно выбрал через AskUserQuestion). Владелец живьём
  сравнил три варианта (0 запаса — реально залезает под «✕ Закрыть», компакт — чисто, старый
  большой `--menu-top-pad` — не тот дизайн). Страж 188 переписан под новую формулу, страж 317
  новый (держит зазор для ВСЕХ явлений сразу, не одного id) — красным поймал баг, зелёным ×3
  подтвердил фикс. Отдельно замечено (НЕ входит в этот фикс, см. открытые линии): собственная
  кнопка «✕ Закрыть» частично визуально спрятана за родными ⌄/⋮ Telegram — не диагностировано.

## Технические hook'и этой ночи (владелец: «везде, где я уже заебался объяснять»)

`.claude/settings.json` + `.claude/hooks/`: guard-full-suite-warn.sh (фоновый полный прогон
стражей без --only= — спрашивает), no-windows-path-redirect.sh (`C:\...` в redirect —
спрашивает), no-broad-taskkill.sh (`taskkill /IM` — блокирует, `/PID` — пропускает),
reinject-session-state.sh (SessionStart/compact — технически возвращает этот файл в контекст).
Все проверены на реальных сценариях до коммита.

## Инциденты «завис ноут» этой ночи — ДВА раза, один и тот же класс ошибки

1. (~40 мин) Фоновый ПОЛНЫЙ прогон стражей (PID 1748) без присмотра.
2. (~20+ мин, ПОСЛЕ того как hook №1 уже стоял) `--only=Workshop` (19 стражей, не полный
   прогон) как обычный передний план — не уложился в 120с харнесса, харнес САМ увёл его в фон
   мимо hook'а (hook проверяет входной run_in_background, а не последующее авто-фоновение),
   10 параллельных chrome-headless-shell.exe (~1.9ГБ) при ОДНОВРЕМЕННО открытой браузерной
   панели. Оба раза — `tasklist`+`taskkill /F /PID <конкретный>`, машина отпускало сразу.
   **Реальное железо (измерено, не оценка): Intel i3-7130U, 2 ядра/4 потока, 12ГБ RAM,
   ~2.4ГБ свободно в обычный момент** — см. [[feedback_slaboe_zhelezo_realnye_cifry_nagruzki]],
   новое ABSOLUTE-правило, владелец назвал основой: один Chromium-тяжёлый процесс за раз,
   закрывать браузерную панель перед любым гвард-прогоном, даже `--only=`.

## Технический факт про подключённый телефон (cf3beda0, OPPO CPH2631)

`adb screencap` даёт чёрный кадр на экране блокировки (FLAG_SECURE — ограничение Android, не
баг инструмента). CDP eval работает независимо от блокировки экрана. `screen_off_timeout`
увеличен до 1 часа. При проверках через CDP eval — ВСЕГДА сначала `toMenu(); renderHangar();
setScreen('hangar');` перед `angarPvZoomOpen(...)`, иначе `angarPvZoomDraw()` тихо не рисует.
Владелец телефон разблокирует сам вручную по просьбе — я не пытаюсь и не должен пытаться
разблокировать чужой экран технически (спросил один раз, получил явное «нет надо мной решать»).

## Владелец ожидает скорое сжатие диалога (его прямые слова, 19.09.2026)

Прямо попросил зафиксировать всё в этот файл ПЕРЕД сжатием, чтобы работа не терялась и не
начиналась «будто с нуля». Это и есть цель этого файла и hook'а reinject-session-state.sh —
после сжатия он вернётся в контекст автоматически, техническим путём, не по памяти.
