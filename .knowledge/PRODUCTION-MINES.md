# 💣 Будущие мины — что решить заранее, пока масштаб маленький

> Источник: разовое исследование 05.09.2026 по прямой просьбе владельца
> ("изучи это... используй интернет") — какие категории проблем обычно
> ломают маленькие команды/соло-разработчиков, когда проект начинает расти,
> и что из этого уже актуально для Cosmogram прямо сейчас. Реальные источники
> у каждого пункта, не по памяти. Это КАТАЛОГ решений-на-будущее, не боевой
> реестр багов (тот — KNOWN-BUGS.md/ERROR-CATALOG.md).
>
> Формат каждого пункта: что нашли → применимо ли к нам прямо сейчас (проверено,
> не угадано) → что нужно РЕШИТЬ владельцу (если нужно решать) → источники.

---

## 🔴 Реальная, подтверждённая дыра — не гипотеза (05.09.2026)

Проверено через `get_advisors` (штатный линтер Supabase) + прочитаны тела функций
через `execute_sql`, не угадано по описанию.

**4 SECURITY DEFINER функции в public-схеме доступны напрямую через REST API
любому анониму, в обход Edge Function и её проверки личности/лимитов:**

| Функция | Что делает | Реальная эксплуатация |
|---|---|---|
| `increment_workshop_plays(p_code)` | `plays = plays+1` без единой проверки внутри | Любой может дёргать `/rest/v1/rpc/increment_workshop_plays` напрямую с любым кодом — накрутка «Играли больше всех» до бесконечности, без Telegram-аккаунта вообще |
| `check_rate_limit(key, window, limit)` | Общий анти-спам счётчик (используется в cosmogram-daily/sync) | Ключ — просто строка, приходит от вызывающего. Аноним может слать чужие ключи (если они угадываемы) и искусственно исчерпывать лимит НАСТОЯЩЕГО игрока — тихий отказ в обслуживании |
| `check_and_use_nonce(nonce, player_id)` | Анти-replay (одноразовые токены) | Аноним может заранее «сжечь» нужный nonce за игрока — его настоящий следующий легитимный запрос откажет как «уже использовано» |
| `rls_auto_enable()` | Служебный event-trigger (сам чинит RLS на новых таблицах) | Скорее всего просто упадёт с ошибкой при прямом вызове вне event trigger — но зря выставлен наружу |

Все четыре — с `SECURITY DEFINER`, что и делает их доступными по RPC широкому
кругу ролей по умолчанию.

**ПОЧИНЕНО 05.09.2026** (владелец подтвердил "да, почини сейчас"). Первая
попытка `REVOKE EXECUTE ... FROM anon, authenticated` НЕ сработала — реальный
грант висел на псевдо-роли `PUBLIC` (Postgres по умолчанию даёт `EXECUTE` всем
при создании функции), а отзыв у конкретных ролей грант `PUBLIC` не отменяет —
классическая и малоизвестная ловушка Postgres, сам себя поймал `information_schema.role_routine_grants`
после первого фикса ещё показывал те же гранты. Правильный фикс —
`REVOKE EXECUTE ... FROM PUBLIC` + явный `GRANT ... TO service_role` (Edge
Function продолжает работать через свой ключ). Проверено ДВА раза: (1) сам
grant-table (`role_routine_grants`) — остались только `postgres`/`service_role`;
(2) живой curl с настоящим анонимным ключом на `increment_workshop_plays` —
`{"code":"42501","message":"permission denied for function..."}`, было `{"ok":true}`
до фикса. Edge Function `cosmogram-workshop` после фикса отвечает как прежде.

**Остальные ~20 предупреждений линтера ("RLS enabled no policy" на каждой
таблице) — проверено и это НЕ дыра**: RLS включён, политик нет → PostgREST
никому ничего не отдаёт напрямую по этим таблицам ни для anon, ни для
authenticated. Ровно то состояние, которое нам и нужно — все чтения/записи
идут только через service_role внутри Edge Function. Один пункт всё же спорный:
`forge_workshop_view` — вид с `SECURITY DEFINER` (уровень ERROR у линтера,
не INFO). Раз им пользуется только наш же Edge Function через service_role —
эксплуатации сейчас нет, но при RLS-без-политик-на-таблице это ровно тот
паттерн, который линтер просит проверить в первую очередь (`security_invoker`).

**ПОЧИНЕНО 06.09.2026**: `alter view public.forge_workshop_view set
(security_invoker = on);` — один шаг миграции. Линтер (`get_advisors`)
перепроверен после — пункт ERROR исчез. Живой запрос к `cosmogram-workshop`
(`list`) подтвердил, что витрина Мастерской отвечает как прежде.

---

## 🟡 Общие категории — проверено по каждой, что у нас уже есть, а чего нет

### 1. «Никогда не доверяй клиенту»
Базовый принцип бэкендов игр: клиент можно модифицировать целиком — память,
трафик, память рантайма — сервер обязан быть единственным источником истины
по критичным решениям ([Hacker News: Never trust the client](https://news.ycombinator.com/item?id=11583008), [Imperva: fundamental security concepts for game devs](https://www.imperva.com/blog/fundamental-security-concepts-and-best-practices-every-game-developer-should-know/)).
**У нас уже так** для Мастерской (bad-word фильтр дублирован на сервере,
05.09.2026, это ровно та находка, что запустила всю сессию багхантинга). Дыра
из раздела выше — тот же класс, только зашедший с неожиданной стороны (не через
Edge Function, а в обход неё через голый RPC).

### 2. Специфика Telegram Mini Apps
Свежее (2026) исследование безопасности мини-приложений TENET: 57% проверенных
приложений хранят реплеемые токены сессий в открытом виде; частая причина —
разработчик отключает проверку `initData` на этапе разработки и забывает включить
обратно ([arXiv TENET](https://arxiv.org/html/2608.17538)). Общая рекомендация
индустрии — окно валидности `auth_date` ~5 минут против replay-атак ([QuillAudits](https://www.quillaudits.com/blog/web3-security/telegram-mini-apps-security), [DEV: validate initData](https://dev.to/serhii_a9c08345ac360cf5c8/validate-telegram-mini-app-initdata-in-php-hmac-sha-256-timing-safe-compare-and-authdate-expiry-4c92)).
**ПОЧИНЕНО 06.09.2026** (владелец подтвердил после уточняющего вопроса — не
про дуэли, про то, сколько живёт украденная подпись). Было `AUTH_MAX_AGE_SEC
= 7*86400` в каждой Edge Function, сокращено до `5*60`. Задело 4 функции:
`cosmogram-sync`, `cosmogram-daily`, `cosmogram-workshop`, `cosmogram-privacy`
(остальные три — `beacon`/`top`/`unsub` — этой константы не используют,
проверено чтением их исходников, не предположено). В `cosmogram-sync` та же
константа ОШИБОЧНО делила имя ещё и со сроком жизни веб-сессии Discord/Google
(`makeSession()`) — механическая замена сломала бы вход через веб каждые 5
минут; пойман до деплоя перечитыванием всех мест использования, разведено на
`INITDATA_MAX_AGE_SEC` (5 минут) и `SESSION_LIFETIME_SEC` (осталось 7 дней,
осознанно). Все 4 функции задеплоены через `supabase` CLI (`functions
download`/`deploy` — читает/пишет прямо с диска, без риска ручной
перепечатки крупных файлов), содержимое сверено побайтово, живой запрос к
каждой подтвердил отсутствие сбоя.

### 3. Платежи Stars — идемпотентность и возвраты
Стандартная практика: хранить `telegram_payment_charge_id` с уникальным
ограничением в базе — при повторном событии `successful_payment` (Telegram
иногда шлёт дубликаты) второй insert должен упасть на UNIQUE и вернуть уже
существующую запись, не начислять товар дважды; тот же id нужен позже для
`refundStarPayment` ([Telegram Bot Payments API](https://core.telegram.org/bots/payments-stars), [n8n: Stars refund workflow](https://n8n.io/workflows/2461-telegram-payment-invoicing-and-refund-workflow-for-stars/)).
**У нас**: реальные Stars-покупки ещё не запущены (монетизация отложена как
отдельный разговор, см. [[project_monetizatsiya_dosug_obsuzhdenie]]) — но таблица
`premium_purchases` уже существует в базе. Когда монетизация дойдёт до кода —
это готовый чек-лист на день реализации, не сейчас.

### 4. Ограничение частоты / накрутка ботами
Слой должен быть не один: IP — это минимум, для авторизованных API работает
лучше привязка к identity + поведенческие сигналы; нужен и «взрывной» лимит на
короткий всплеск, и «длинный» на суммарное использование ([Stytch: API rate limiting](https://stytch.com/blog/api-rate-limiting/), [Zuplo: protect APIs from bots](https://zuplo.com/learning-center/how-to-protect-your-apis-from-automated-bots-and-attacks)).
**У нас**: `check_rate_limit`/`MIN_SUBMIT_INTERVAL_MS`/`DAILY_SUBMIT_LIMIT`
есть для публикации трасс — но у `vote`/`played` в Мастерской лимита нет вообще
(частично компенсируется тем, что vote — уникальный ключ игрок+код, а played
до сегодняшней проверки был вообще без всякой защиты — см. дыру выше).

### 5. Рост таблиц без архивации
Стандартная рекомендация — не хранить бесконечно, архивировать историю отдельно
от рабочих данных заранее, не когда база уже разрослась ([OneNine: database growth issues](https://onenine.com/common-database-growth-issues-and-fixes/), [AppSignal: table bloat](https://blog.appsignal.com/2022/02/03/how-to-keep-database-table-sizes-down-and-prevent-data-bloat.html)).
**У нас**: `forge_workshop`, `track_votes`, diag-скриншоты — уже отмечено в
памяти как «пересмотреть при росте в 10 раз» ([[project_diag_shot_cost_cleanup]]) —
осознанное решение, не забытое, менять сейчас не нужно.

### 6. Резервное копирование / восстановление после сбоя
PITR (point-in-time recovery) называется «самой важной» настройкой на случай
сбоя — без неё теряются все записи между последним бэкапом и инцидентом
([TigerData: PostgreSQL backups](https://www.tigerdata.com/blog/database-backups-and-disaster-recovery-in-postgresql-your-questions-answered)).
**Не проверено мной** — это настройка тарифного плана Supabase-проекта, не
видна через доступные мне инструменты (SQL/advisors её не показывают). Честно:
не знаю, включён ли PITR на `cwpijvgdrrvnvldhnmbj» — это смотрится в Supabase
Dashboard → Database → Backups владельцем напрямую, я туда доступа не имею.

### 7. Service Worker — что если новая версия зависает у части игроков
Частая ловушка — HTML закэширован Cache First, из-за чего update невидим
пользователю сколько угодно долго; лечится версионированием кэша по билду и
явной очисткой старых кэшей на `activate` ([Hasura: SW caching strategies](https://hasura.io/blog/strategies-for-service-worker-caching-d66f3c828433), [Infinity Interactive: taming PWA cache](https://iinteractive.com/resources/blog/taming-pwa-cache-behavior)).
**Проверено, у нас уже правильно**: `sw.js` делает `skipWaiting()` на install
и чистит все чужие ключи кэша на `activate` — ровно рекомендуемый паттерн,
чинить нечего.

### 8. GDPR-K / COPPA (дети 3+)
Апрель 2026 — обновлённые правила COPPA вступают в силу, требования к
родительскому согласию и сбору данных ужесточаются ([gamesbeat: updated COPPA rules](https://gamesbeat.com/understanding-the-updated-coppa-rules-and-their-impact-on-child-safety/), [SuperAwesome: COPPA/GDPR-K for game devs](https://www.superawesome.com/blog/5-things-game-developers-need-to-know-about-coppa-and-gdpr-k/)).
**У нас**: контентная политика 3+ уже есть ([[feedback_content_policy_kids]]),
но это про КОНТЕНТ, не про СБОР ДАННЫХ — а телеметрия (`beacons`) собирается
по умолчанию всем. Это ровно тот пункт, что уже стоит открытым в реестре решений
как S12 «нужен юрист» ([[project_telemetriya_opt_in_status]]) — не новая
находка, но эта самая юридическая тема прямо перекликается с COPPA-апрелем.
Не додумываю юридический вывод сам — это открытый вопрос реестра, не мой звонок.

### 9. Соло/маленькая команда — типичные организационные мины
Почти каждый разбор провала инди-команды начинается с масштаба фичи, а не
с бага: команда без роадмапа реагирует на срочность вместо приоритета
([itch.io post-mortems index](https://danbruno.net/notes/games/failure-postmortems/), [GameDeveloper: 9 common indie mistakes](https://www.gamedeveloper.com/design/the-9-common-mistakes-every-indie-game-studio-should-avoid)).
Это не техническая мина, это процессная — не моя зона (я не решаю приоритеты),
просто фиксирую саму категорию как честно найденную по просьбе.

---

### 10. Монетизация — только Stars, не мимо
Для цифровых товаров внутри Mini App Telegram разрешает только Stars — это не
рекомендация, а условие: обход через внешний платёжный шлюз рискует удалением
приложения с площадки. Отдельно: с начала 2025 блокчейн-функции в Mini Apps
разрешены только на сети TON — Ethereum/BNB ведут к снятию с публикации
([Scrile: Telegram monetization 2026](https://www.scrile.com/blog/telegram-monetization), [Merge: Mini Apps 2026 monetization guide](https://merge.rocks/blog/telegram-mini-apps-2026-monetization-guide-how-to-earn-from-telegram-mini-apps)).
**У нас**: реальная монетизация ещё не начата (см. [[project_monetizatsiya_dosug_obsuzhdenie]],
[[project_nebo_blagodarnosti_stars]]) — но раз разговор про «100% реальные деньги
за тюнинг» уже был, важно держать в голове ЗАРАНЕЕ: что бы ни решили продавать —
только через Stars, никакого «переведи мне на карту» или стороннего эквайринга
внутри самого Mini App.

### 11. Стоимость облака при абьюзе — не только безопасность, ещё и счёт
Supabase считает деньги за КАЖДЫЙ вызов Edge Function, включая упавшие с
ошибкой и холодный старт; бесплатный тариф — экспериментальная площадка,
не то, на чём стоит держать боевой проект ([Supabase Edge Function invocations billing](https://supabase.com/docs/guides/platform/manage-your-usage/edge-function-invocations), [Sashido: what breaks first](https://www.sashido.io/en/blog/supabase-pricing-ai-prototypes-what-breaks-first)).
Платный тариф Supabase включает лимит-по-умолчанию (spend cap) как защиту от
неожиданного счёта.
**Не проверено мной** — какой тариф сейчас у `cwpijvgdrrvnvldhnmbj` и включён
ли spend cap, не видно ни одним доступным мне инструментом (только
Dashboard → Settings → Billing). Честно говорю: не знаю, владельцу лучше
свериться самому — особенно теперь, когда дыра из раздела 🔴 закрыта, но
общий принцип «чужой скрипт может генерировать вызовы за наш счёт» стоит
держать в голове на будущее при любой новой публичной Edge Function.

### 12. «Фактор автобуса» — команда из одного-двух человек
Стандартный термин: если единственный человек, который знает архитектуру,
пароли и process деплоя, внезапно недоступен (болезнь, потеря доступа, смена
приоритетов) — проект останавливается целиком. Устойчивость обычно строят так,
чтобы знание было свойством СИСТЕМЫ (документация, чистая архитектура), а не
одной головы ([Indie Catastrophe: bus factor](https://indiecatastrophe.com/topics/busfactor/), [Founders Bar: bus factor explained](https://foundersbar.com/articles-and-research/bus-factor-explained-silent-startup-killer)).
**У нас**: `.knowledge/` — это уже осознанная попытка сделать знание свойством
системы, не только моей памяти или головы владельца — сильная сторона, редкая
для соло-проекта. Но чисто организационные вещи вне моей видимости — есть ли
у кого-то ещё доступ к аккаунту Supabase/GitHub/Telegram-боту, есть ли
резервная копия `bot_token`/`service_role` ключей в надёжном месте отдельно
от рабочего компьютера — это не код, я туда не вижу и не додумываю, но
называю прямо: если ответ «нет ни у кого, кроме меня, и нигде не записано
отдельно» — это ровно тот риск, который стоит закрыть, пока цена низкая
(маленький проект), а не когда он вырастет.

### 13. Право игрока на удаление данных — проверено, реального механизма НЕТ
GDPR (и многие похожие законы) требуют отвечать на запрос удаления личных
данных без неоправданной задержки, максимум в течение месяца; запрос не
обязан быть формальным письмом — устного «удалите мои данные» уже достаточно
([Jetico: right to erasure guide](https://jetico.com/blog/how-right-erasure-applied-under-gdpr-complete-guide-organizational-compliance/), [Reform: right to erasure compliance steps](https://www.reform.app/blog/right-to-erasure-gdpr-compliance-steps)).
**Проверено по коду, не угадано**: ни в одном js-файле нет функции удаления
или экспорта данных игрока — ни на клиенте, ни как действие в каком-либо
Edge Function. Данные при этом собираются реальные: `players`(имя/username),
`identities`, `scores`/`daily_runs`/`speedrun_runs`, `beacons`(телеметрия),
`forge_workshop`(если игрок что-то опубликовал). Если завтра игрок попросит
«удалите меня» — сейчас ответить на это нечем, кроме ручного SQL от владельца
через меня. Это не критично при друзьях-игроках, но ровно тот пункт, который
дешевле решить сейчас (одна функция «удалить меня» в Edge Function + кнопка в
настройках), чем когда игроков станет много и запросы пойдут потоком — решение
делать или нет, не додумываю за владельца.

### 14. Что уже сделано правильно, стоит знать почему (не менять)
`js/vendor/telegram-web-app.js` — проверено, лежит ЛОКАЛЬНО в репозитории, не
подключается с чужого CDN/домена Telegram. Это осознанно защищает от риска
«Telegram обновил свой мост и что-то сломал у нас без предупреждения» — типичная
supply-chain ловушка для проектов, которые тянут сторонний скрипт напрямую по
ссылке. Менять на CDN-подключение не нужно, наоборот — держаться этого паттерна
и для любых будущих сторонних библиотек.

---

## Что дальше
1. ~~REVOKE EXECUTE у 4 функций~~ — **сделано 05.09.2026**, см. выше.
2. ~~Окно `AUTH_MAX_AGE_SEC`~~ — **сделано 06.09.2026**, см. выше.
3. PITR-бэкапы и тариф/spend cap Supabase — владельцу проверить самому в
   Dashboard, я туда не вижу. Тариф сейчас бесплатный (владелец подтвердил
   06.09.2026) — вопрос неактуален, пока не перейдёт на платный.
4. ~~`forge_workshop_view` security_invoker~~ — **сделано 06.09.2026**, см. выше.
5. ~~«Удалите мои данные»~~ — **сделано 06.09.2026**, commit 6d7d923
   (v1.478.92), см. [[project_udalit_moi_dannye]].
6. Резервный доступ к ключам/аккаунтам (bus factor) — организационный вопрос,
   не код, объяснено владельцу 06.09.2026, дальше решать ему самому.
7. Остальное — на будущее, без дедлайна, не блокирует ничего сейчас.

**Все пункты, требовавшие явного решения владельца в этом заходе, закрыты.**
Процессные уроки этой сессии (grant/revoke верификация, поиск всех мест
использования константы перед правкой) записаны в CLAUDE.md, не только здесь —
чтобы применялись автоматически в любой будущей сессии, не только вспоминались
при чтении этого каталога.
