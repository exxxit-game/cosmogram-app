# 🎇 Аудит Вспышек и Следов — 11.09.2026

> Тот же проход, что `SKIN-FX-OPTIMIZATION.md` (09.09.2026), только для двух других каталогов
> узоров: `renderFlashPattern` (131 стиль, «Вспышка») и `renderTrailPattern`/`drawStructuredTrail`
> (16 стилей, «След»). Методика измерения и чёрный список примитивов — там же, здесь не
> повторяется, только результат конкретно для этих двух функций.

---

## 1. Чёрный список примитивов — чисто

Ни `renderFlashPattern` (1370 строк), ни `renderTrailPattern` (205 строк), ни
`drawStructuredTrail` не содержат `shadowBlur`, `ctx.filter`, `getImageData`/`putImageData`
внутри кадра, повторного `createPattern`. Ничего чинить в этой категории не нужно было.

## 2. Измерение — все 131+16 узоров, живьём (7×50, медиана)

**Вспышки, живьём через `renderFlashPattern`** — худший результат **0.174мс** (Папоротник,
`lsysFern`), дальше резкий спад: Треугольная мандала 0.09, Шляпа и Метатрон 0.066, Куб
Метатрона 0.054 — и почти все остальные (110+ из 131) ниже 0.03мс. При бюджете кадра ~10мс
даже худший случай — запас ×57.

**Следы — два разных пути в реальном полёте, оба измерены отдельно:**
- `drawStructuredTrail()` (11 «связных» стилей — Лента/Нить-жемчуг/Узел-петля/Волна-змейка/
  Сердце-узел/Созвездие-след/Скрепка/Радуга-арка/Волны/Кельтский жгут/Кельтская коса) —
  измерено НАСТОЯЩЕЙ функцией полёта с реальным `trailHistBuf` (28 точек), не через
  `renderTrailPattern` (та используется только в превью Ангара/явлении, не в самом полёте —
  разные функции для одних и тех же имён). Худший результат — **Кельтская коса 0.166мс**,
  Кельтский жгут 0.122мс, дальше всё ниже 0.09мс. Запас — ×60 даже на худшем.
- Искры/Кометная пыль/Обломки-спутники (`p.trailFx` в системе частиц, `drawFx()`) — один
  `fillStyle`+`arc`/`path`+`fill` на частицу, не дороже соседних незапертых веток того же
  цикла — измерение отдельной цифрой не потребовалось, сложность идентична уже работающим
  без порога частицам рядом.

## 3. Найдено и исправлено — тот же порог, что был у скинов до 09.09.2026

**Было:** 15 из 16 следов (11 через `STRUCTURED_TRAILS`, ещё 3 — Искры/Кометная пыль/
Обломки-спутники) рисовались только при `hq` (`Q.level>=2`, «Высокий» и выше). На «дне» и
«Среднем» (Q.level 0/1) — вообще без следа, ровная пустота позади борта. Единственный
незапертый — Метки пути (`waypoints`).

**Стало:** порог `hq &&` снят у всех 4 мест (`js/render.js`: блок `STRUCTURED_TRAILS`, и
три ветки `p.trailFx` в `drawFx()`) — после измерения он не был подтверждён числом, ровно
как и у скинов. Вспышки порога не имели вообще (`drawLaunchFlash()` и превью в Ангаре) —
там чинить было нечего, только след.

Проверено живьём на Q.level=0 с самым дорогим следом (Кельтская коса) — виден, полёт не
проседает.

## 4. Осталось не решено (как и у скинов)

Замер — настольный Chromium. Живого подтверждения на Samsung A3 Core владельца по-прежнему
нет — тот же открытый пункт, что в `SKIN-FX-OPTIMIZATION.md` §5.

---

Стражи: 200/200. Коммит — `js/render.js` (снятие `hq`-порога, 4 места).
