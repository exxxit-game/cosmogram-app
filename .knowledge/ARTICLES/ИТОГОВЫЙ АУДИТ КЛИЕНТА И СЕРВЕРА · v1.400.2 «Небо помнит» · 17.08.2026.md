# КОСМОГРАММА v1.400.2 — ИТОГОВЫЙ АУДИТ (17.08.2026)

## Охват
- Клиент (18 файлов), сервер (3 боевые Supabase + 2 отключённые заглушки 410 + worker.js),
  sw.js, тесты guard.mjs + relizy.mjs — прочитаны целиком.
- mirror-sync.ts и ct-test.ts НЕ существуют (подтверждено владельцем). index.ts (5)=(6) — дубликат щупа.

## Архитектура (подтверждено кодом)
- Каскад 18 defer-скриптов ?v=1.400.2; свідный порядок; маяк __gameUp = живность.
- Хартия неба: STEP 1/60 · 4 подшага · сид+лента (подпоток тратится на каждый спавн) ·
  метр 390×844 · коридор 390 мер · потолок 2560 · пулы · Zero-GC в тике.
- 5 категорий: gyro/touch/keys/bullet/dist. PWA выключено (воркер есть, не зарегистрирован).
- Supabase Edge verify_jwt:false, защита внутри кода; JWT не используется; CORS *.

## Сервер
- cosmogram-sync: три личности; паспорт забега (run: score/dist/sec/seed/mode/restored/v);
  порядок «рекорд → призрак»; days_ack по слову сервера; share_ghost v23 — выкл только прячет.
- cosmogram-daily: окно даты ±1.5/+2.5 суток; SCORE_CAP 10M; антиспам 5с; лента 60..4200/кратна 3/[!-~];
  знак не сгорает; champion — первый с лентой и share_ghost!==false; дверь fly_first у stats/champion.
- cosmogram-top: только чтение, 5 категорий, LIMIT 100, форма ответа = топ-sync; ВРЕМЕННАЯ.
- Закрытый синк (410) и Забытый щуп (410): заглушки, удаление через панель Supabase. НЕ ЧИНИТЬ.
- worker.js: CosmoCoder Proxy (Anthropic), DEV_KEY, constant-time, потолки 256КБ/60, MODEL, НЕ игровой модуль.

## Клиент — главное по модулям
- core: GRACE_LIMIT=3, _mergeDisk (максимумы, не затирание), dayJournal (день на взлёте),
  tgPerfClass, Store.CLOUD_KEYS, canvas alpha:false, contextloss/restored, gfxCeil, 1/60 фикс.
- input: поканальный ноль (корень cal_storm), «суд компасов», maySteer порядок, руль не залипает
  в калибровке, второй палец не угоняет, клавиши уважают поля, Ctrl+W не залипает.
- game: волна только по дистанции, waveDistTarget −12.5% (партия 23), lull-передышки (26/28,
  крючок 1-й волны ×1), вертикальный коридор, ghostAgain 0-3, занавес смерти не в паузу.
- ui: runGen/runNow/runSame; «Одна попытка» дня (журнал dailyDone); tapNaKnopku (148); автосейв своей
  дисциплины; Ангар-витрина (112); дуэль TTL 30д; Гость виден (topWouldBe); лента без тумблера (128).
- index.html: CSS инлайн (1.88.0), подушка --sat/--sat-menu, спиннер по маяку, #tooNarrow z-40,
  фильтры hc.cb (121), запас 96/56 (130), #setGhostsBtn БЕЗ слушателя (долг).
- render: gfxCeil в Store (49), FPS только в небе, gradCache, Path2D-кэш камня, partCol 40 ступеней,
  gfxInvalidate→PLANET/GOLD._gfxReset (23).
- beacon: окно 21000, поводок 8с, quiet=НЕ доставлено, дедуп без fps, calTick(takeoff не шторм).
- ach/planetarium/goldstar/music/gyro/forge/card/star: 6 бонусов, станция 12–38с, GOLD_DIST,
  DRONE_ROOTS-дрейф и jitter-приём Ино, статус-звезда 259200с, карточка чат/сторис.

## Тест-стенд
- Реестр 0–138 (без номера 132), стражи сначала красные, потом зелёные; node tests/guard.mjs [--only=];
  глобальный Playwright; CDN block, __labOpen/__beaconLab; 104 (не вернуть Sentry/Amplitude);
  96 (версия единая); 133/137 (ключи и раздача; читают relizy.mjs).

## Долги (правки только с «да» владельца)
1. #setGhostsBtn без обработчика. 2. Обещанная «проверка версии на сервере» в index.html не найдена.
3. Кэши градиентов PLANET/GOLD не пересоздаются при W/H без contextlost. 4. PK daily_runs не оговорён.
5. applyLang — монолит. 6. cosmogram-top — временная, удалить после переноса.
7. «Семь edge-функций» в SYSTEM_PROMPT → фактически 3 боевые + 2 заглушки.
8. Стенд требует tests/relizy.mjs (теперь в наборе есть) и глобального playwright.

## Известные закрытые (подтверждено)
- K3 призрак после рекорда — закрыт порядком submit→ghost; K4 — beacon (quiet=не доставлено);
  K5 — Store разгружает при переполнении; категория Keys в 1.280.0; сид в рекорде (127);
  лента как доказательство (128); дневник гостя анонимным каналом (105).